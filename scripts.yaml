scripts:

  build: elmstatic build

  ssh: ssh root@206.189.184.194

  publish:
    desc: publish to elmstatic blog site
    cmd:
      - npx elmstatic build
      - rsync -a _site/ root@206.189.184.194:/var/www/jxxcarlson.io/html/
      - git commit -am "Update text"
      - git push

  go: open -a /Applications/Firefox.app/ https://jxxcarlson.io
